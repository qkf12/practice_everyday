drop
		是删除表的意思
			例如
				drop table if exists `user`;
if exists
		如果数据库里有这个表 那就删掉
			如果没有 就直接跳过  不会报错
`user`
		用`防止与关键字冲突
bigint  
		也是数字属性 跟int  double  一样

alter table 表名 column 列名 加各种属性和约束
		alter table  `user` add column age int null default null comment "年龄";

		添加表的莫一列
		删除表的某一列  把add 换成drop 并把属性删掉
		alter  table `user` drop column email;




新版本的jdk  不能直接打印连接到的数据库
必须新增一个
		环境
			<plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>


全部添加到  plugin  并列


server:
  port:			//不是post  并且 缩进关系是 server下面的
    8081
  servlet:
    context-path: /dev-api



spring:
  datasource:
    url: "jdbc:mysql://127.0.0.1:3306/spring_boot6"
    username: "root"
    password: "Qk."
    driver-class-name: com.mysql.cj.jdbc.Driver

		上面的url  username  等 后面都不用加  ；  因为不是java文件是yml



import org.example.spring_boot6.entity.User;

这个是usermapper 接口文件  里的导入行

是跟路径有关的  


同一行代码 在启动文件里 也有 跟路径有关  等以后解密

import org.apache.catalina.User;          // 这是 Tomcat 的接口，不是你的实体





数据库中删除一行数据 是
		delete from 表名 where  列名 = ！！；

添加完数据 之后 只要 莫一行有一个数值  那就只能  用update 

		update `user`  set !! = !!, !! = !! where = !!;


	属性是  datetime  
		时间嘛   now（）
		update `user` 
		set create_time = now(),update_time = now() 
		where id != 1;
		



重中之重是

  每一次导入user的时候都要更改一下配置



@GetMapping
    public List<User> getAll() {
        return userMapper.selectList(null);
    }

		这个是获取到数据库所有信息的代码    跟 知识点 的不同

	太多专有名词了  
		


CREATE TABLE t_user_role (
  user_id int NOT NULL,
  role_id int NOT NULL,
  PRIMARY KEY (user_id, role_id)  -- 必须写在最后，组合在一起
);

这个主键是      PRIMARY KEY   写到这一行是 可以快速 删除或者更改
	可以给两个值给出主键



COLLATE utf8mb4_unicode_ci：
			排序规则，_ci 代表 Case Insensitive（不区分大小写）。utf8mb4 支持存储中文和 Emoji 表情。

























